# Site
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Optional — without these, pages save to .data/pages.json and photos to .data/images
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional — without these, "Keep forever" marks the page kept instantly (dev)
POLAR_ACCESS_TOKEN=
POLAR_PRODUCT_KEEP=
POLAR_WEBHOOK_SECRET=
# sandbox | production
POLAR_SERVER=sandbox

# Protects /api/cron/expire on Vercel
CRON_SECRET=
