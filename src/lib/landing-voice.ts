/** Approved landing hero — curated master v2 source of truth. */
export const LANDING_HERO_LINES = [
  "pity is a",
  "terrible",
  "religion",
] as const;

export const LANDING_ATMOSPHERE_TEXT = LANDING_HERO_LINES.join("\n");

/** Name-the-inbox whisper above the serif title. */
export const NAME_INBOX_WHISPER = "victim mindsets self-destruct";

/** Login backdrop whisper — stacked, faint, behind the tray. */
export const LOGIN_WHISPER_LINES = [
  "the cage",
  "learns",
  "your shape",
] as const;

export const LOGIN_WHISPER = LOGIN_WHISPER_LINES.join(" ");

/** Terms sidebar whisper. */
export const TERMS_WHISPER_LINES = ["resentment", "keeps", "receipts"] as const;

/** Kept. ownership beat. */
export const KEPT_WHISPER = "kept.";

/** Public-page scarcity caption example — page content is owner-authored. */
export const PUBLIC_CAPTION_EXAMPLE = "the wound wins when it becomes you";
